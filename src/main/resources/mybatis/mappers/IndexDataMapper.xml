<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lejia.mapper.IndexDataMapper" >

  <select id="selectByCategoryId" resultType="java.util.HashMap">
    SELECT * FROM  tb_app_Index_Data a WHERE a.index_cat_id =#{indexCatId,jdbcType=INTEGER}
  </select>
</mapper>